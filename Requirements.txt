

# ── Core vision ─────────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
 
# ── Detection: YOLO-World (via Roboflow inference) ───────────
inference[yolo-world]>=0.9.0
supervision>=0.18.0
 
# ── Tracking: DeepSORT ───────────────────────────────────────
# Clone manually:  git clone https://github.com/ZQPei/deep_sort_pytorch
# Then install its requirements inside the cloned folder.
# deep_sort_pytorch is a local package, not on PyPI.
 
# ── Temporal: Extended Kalman Filter ────────────────────────
filterpy>=1.4.5
 
# ── Data / Visualisation ─────────────────────────────────────
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
PyYAML>=6.0
 
# ── VLM Captioning (BLIP-2 or LLaVA) ───────────────────────
transformers>=4.38.0
accelerate>=0.27.0
torch>=2.0.0            # GPU recommended; CPU fallback works
torchvision>=0.15.0
 
# ── Caption Evaluation ───────────────────────────────────────
nltk>=3.8.0
rouge-score>=0.1.2
evaluate>=0.4.0         # optional – HuggingFace evaluate library
 
# ── Tracking Metrics (MOTA/HOTA) – optional ─────────────────
# py-motmetrics>=1.2.0
# trackeval requires manual install from:
#   https://github.com/JonathonLuiten/TrackEval
 






